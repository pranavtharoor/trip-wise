import TripList from './tripList.container';
import reducer from './tripList.reducer';
import saga from './tripList.sagas';

export const tripListReducer = reducer;
export const tripListSaga = saga;

export default TripList;
