import TripUsersList from './tripUsersList.container';
import reducer from './tripUsersList.reducer';
import saga from './tripUsersList.sagas';

export const tripUsersListReducer = reducer;
export const tripUsersListSaga = saga;

export default TripUsersList;
